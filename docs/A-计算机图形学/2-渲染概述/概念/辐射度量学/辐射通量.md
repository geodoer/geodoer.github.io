辐射通量$\Phi$表示的是一种 **光源所输出的能量**，以瓦特为单位。

可见光

- 波长介于390nm到700nm（纳米）的光被认为是处于可见光光谱中，也就是说它们是人眼可见的波长
- 在图形学中，主要目的是渲染，为人眼所服务，因此默认讨论的都是“可见光”
- 下图展示了，日光中不同波长的光所具有的能量

![](images/可见光.png)

光 == 不同波长能量之“和”

- 光是由多种不同波长的能量所集合而成的，而每种波长则与一种颜色相关
- 因此，一个光源所放射出来的能量可以被视作这个光源包含的所有各种波长的一个函数

通常，我们不直接使用波长的强度，而是 **使用三原色编码** 来简化它，也就是RGB（或称：光色）

- 辐射通量将会计算 这个由不同波长构成的函数 的总面积（波长函数的积分？）
- 直接将这种不同波长的计量作为参数输入图形学有一些不切实际。因此我们用RGB简化它
- 三原色编码确实会带来一些信息上的损失，但这在视觉效果上的影响基本可以忽略
